# 夜間・直列実装の継続ログ

## 作業場所と権限

- `C:/---hp/_worktrees/keio-2025-mathematics` / `codex/keio-2025-mathematics`。
- 元HTML：`C:/---hp/shidai-igakubu-gokaku-dokuhon`。このセッションでは読み取り専用。
- ステージングへの即時pushはユーザー承認済み。本番・mainへのpushはしない。
- 別担当と並列エージェントを起動せず、大学・科目単位で直列に進む。
- このタスクの夜間再開をアプリで設定済み。期限2026-09-10 08:00 JST。状態だけ眺めず、可能な図版・ページ実装を進める。

## 2026-09-09 初回一括取り込み

- `cc22e79d`で30大学67パッケージ201ルートをstagingへpush。Cloudflare成功。
- 全201実URLのHTTP 200、h1が1個、noindex、canonicalを確認。`qa/past-exam-batch/staging-http.json`。
- 全201ローカルページをPCと390pxモバイルで実際に開いた機械検査：ページ横幅超過なし、KaTeXエラー/未描画なし、読込済み画像エラーなし。スクリーンショット目視は一部のページ。
- 問題67本文、解説65本文、分析64本文。図版予約427箇所、目標点保留19件から開始。
- 元データ欠損2解説・3分析と、分析計算の不備などは大学別修正依頼へ。未検出の誤りがないという保証はしない。
- `editorial_adaptation_import`は学習者向け既存解説の取り込み。元の独自解説5件を保護し、独立した再解答と偽らない。

## 2026-09-09 愛知医科大学の図版

- 数学4図：3接円、平面への垂線、球の切断円、平面内の円のパラメータ表示。
- 物理6図：2つの電荷配置、ばねの初期状態、長方形位相図、空欄の解答軸、解説用楕円。
- 10図すべて公開問題条件から独自描画。restricted画像のコピー・トレースなし。
- 2つのpackage生成スクリプト、manifest、意味検証テストを追加。SVG構造検証と全PNG/コンタクトシート目視済み。再生成で人の編集を消さないhandoff機構も適用。
- 数学4図はローカル本文への画像読込、PC/モバイル幅の配置を確認。物理の組込み後のbuild・画面確認は下の最新結果で確認すること。
- 取り込み直後の残り図版予約：417箇所。問題207＋解説210。目標点保留19件、本文欠損は変わらない。
- 147回帰テストを一度実行して成功（物理用追加3件は別途成功）。最終の一括テスト結果は次の追記参照。
- 不等号の元ローカルanalysis authoring 15件を既定表記にそろえ、importer側にも正規化を追加。元ソースは変更していない。

## 次の対象と手順

次は `dokkyo-medical-2025-general-early-mathematics`（解説の図14配置）、次いで同大学物理（問題11配置）。`pastExamBatch/status.json`の予約数を確認して順番に進む。条件不足の図は推測せず理由を修正依頼へ残し、その次の描画可能な図へ進んでよい。

1. `.agents/skills/past-exam-staging-batch/SKILL.md`と図版スキルを読む。
2. source snapshotの問題文・cropの説明・解説条件を読む。restricted cropをトレースしない。
3. 数学/物理スキルに従ってpackage generatorとテストを作る。共通の文書・フォント枠は `scripts/lib/past-exam-svg-author.mjs`を利用可。
4. manifestのIDを既存crop IDに一致させる。元ファイルとは別の独自SVGを作り、全図をラスタライズして目視確認。
5. frontendから `node scripts/import-past-exam-batch.mjs C:/---hp/shidai-igakubu-gokaku-dokuhon <library-package-id>`、`npm run build`、関連テスト、`node scripts/audit-past-exam-math.mjs`、`node scripts/report-past-exam-batch.mjs`。
6. PC/モバイルの図・数式、印刷を確認。ブラウザー操作は提供されたCUAを使う。全ページの印刷目視はまだ未完了。
7. fetch後、他担当のstaging差分があるなら先に安全に統合し再検証。通常pushのみ。新しいデプロイを実URLで確認して本ログへ追記。

## 禁止する近道

- 201ルートが存在することを、201ページの内容完成と呼ばない。
- 図を使う説明を消して図の必要性そのものをなくさない。
- 未検証の目標点を一括再計算し、元の計画と違う数値へ黙って差し替えない。
- 元source errorに直面しただけで一括停止しない。該当部分を保留し、他の構築を続ける。
- 無関係なブランチ・他作業ツリーを清掃しない。手で編集されたtrio/SVGは上書きしない。

## 2026-09-09 22:10 JST 追加図版の最終検証

- 愛知の10図・2manifestは再生成でバイト単位に一致。
- `npm run build`成功：1118ページ、Astro 0 errors / 0 warnings（既存3 hints）。
- 数学解説4図・物理問題5図・物理解説1図のローカル読込成功。PC/390pxモバイルにページ横幅超過なし。本文内の球・ばね・位相楕円を目視確認。印刷CSSの回帰はテスト済みだが、今回の10図の印刷プレビュー目視は未実施。
- 公開前レビューは依然必要。全解説の論理検証済みとは言わない。
- 最終回帰テスト151件すべて成功。共通SVG生成枠が手編集済みSVGを実際に上書きせず、その寸法も保持するテストを追加した。
- 全問題・解説の14,651 TeXターゲットを描画検証し、構文エラー0件。数式の正しさの検証とは区別する。
