# 関西医科大学2025前期数学：元HTML修復担当への依頼

対象repoは `C:/---hp/shidai-igakubu-gokaku-dokuhon`。対象は `projects/universities/kansai-medical/past-exams/working/2025/general-early/first-stage/mathematics`、package IDは `kansai-medical-2025-general-early-mathematics`。元repoのAGENTS.md、大学別AGENTS.md、過去問再構成・数学学習者解説・分析の該当スキルを先に読んでください。募集要項2027の承認状態を過去問2025へ流用しないでください。

Lexus担当は問題3ページ・原解答14ページ・学習者14ページ、原本PNG17ページ（問題紙面18〜20／解答79〜92）、分析13小問、derived、分析HTMLを監査しました。元repoは変更していません。全6図は条件と独立計算から新規SVG化しましたが、原本・学習者本文・権利の人間承認とは別です。以下が今回検出した事項の全記録です。未検出不備がない保証ではないので、修復後にも全問再監査してください。

## 1. Ⅰ(1) 放物線の分母が0になる角の説明を復元

原本解答p001には、a>0でθ≠π、a<0でθ≠0を使って割る説明があります。reconstructionとeditorialではこれが省略され、最終的な分数式だけになっています。極角の取り方を明示し、例えば0≦θ<2πならa>0はπ、a<0は0を除くと説明してください。一般の角なら2π周期で表します。r≧0の通常の極座標であることも確認。

円についても、直径に対する円周角∠OPAが直角だからOP=OA cosθとつなぐと図を使う目的が伝わります。r=2cosθを繰り返し別色枠にする必要はありません。原点r=0の確認は本文へ短く含め、削除によって原点が抜ける説明にはしません。

## 2. Ⅰ(2) 方程式変形の同値性と端点

0<x<2πでは1−cosx>0なので分母を払ってよい、と明記してください。h′の積のどちらが0か、sinx=0／cosx=1/2→π,π/3,5π/3を示すと再現できます。原本p002にある途中の微分式を全て色枠にする必要はありませんが、学習者が因数分解と臨界点を追える1つの計算ブロックにすること。

原本p002の増減表では、範囲外の両端に対応するh′のセルに斜線があります。HTMLは空文字のため、承認済みの増減表構造（隅から隅への斜線、二重線は項目列右側だけ）で表現してください。表はSVG画像にしません。k=0では範囲外の0と2πを数えず、π/2と3π/2の2個であると示す。独立計算で個数分類0/1/2/4と境界−8,0,1は一致しており、答えの修正ではありません。

## 3. Ⅱの因数対を式の羅列でなく表へ

学習者p004〜p006は因数対と(m,n)を2組程度ずつ独立色枠に分割し、途中で左辺すら消えています。意味が対応する `n−m / n+m / m / n` のHTML表へまとめ、(3)は `n−m+1 / n+m / m / n` と区別してください。原本の指数による場合の数え方も転記から消えているため、raw転記と編集要約を区別して復元。

- (1)同じ偶奇になる理由は和差を2で割ってm,nへ戻すこと。2024は偶数なので両方偶数。2を両因子から除くと積506=2·11·23になり、8約数を大小で対にして4組。
- (2)2025=3⁴·5²の約数15個。45·45を除き(15−1)/2=7組。m=0が不適となることを示す。
- (3)原本p005の復元式 `m=((n+m)−(n−m+1)+1)/2`、`n=((n+m)+(n−m+1)−1)/2` がraw/学習者から落ちています。因子の偶奇が逆、`1<n−m+1<n+m` の条件から自然数へ戻せると説明する。Ⅱ(1)(2)と同じ和差復元式ではありません。
- 「何通り」を問うので答案で全(m,n)を列挙する必要はない、という原本p006の重要な学習注記も落ちています。表は確認用として置き、必須答案の分量と区別してください。

独立全探索の結果は4／7／14通りで、掲載された全組の数値に誤りはありません。

## 4. Ⅲの条件付き遷移とn=1の境界

初回は負けないこと、継続中なら直前の数字は1以外であることを明記。次回は1の1通りが勝ち、直前と同じ1通りが負け、残るm−2通りが継続だから漸化式となります。p006の学習者は `a_n=b_n=c_{n−1}/m` と次の `c_n=... (n≧2)` を分断し、n≧2が前式にも掛かることが弱くなっています。

p007の(1)(2)は原本にあるn=1の確認が脱落。n≧2で望遠和を示し、n=1も直接成立することを本文で短く確認。計算の分割のためだけにT_nを追加し、定義より先に使う現行構造をやめ、同じaligned内で相殺を見せてください。最終的な差1/m、和1−c_n、一般項、無限和比(m+1)/(m−1)は独立検算と一致しています。

## 5. Ⅳ(2) 増減表の符号セルの位置（確定不備）

reconstructionとeditorial p009のz′行は `['z′','+','','0','−','0','+','']`。原本p009では最初の+は端点−1ではなく区間−1<x<−1/2のセルにあります。`['z′','','+','0','−','0','+','']` の対応へ戻してください。端点の空欄を「未定義」と表記しないこと。z′は端点でも存在しますが、区間の符号を示す欄とは役割が違います。

高さh(x)=4x³−3x+1=4(x+1)(x−1/2)²は−1≦x≦1で非負。z′=0は±1/2であり、グラフと表の値0,2,0,2は一致しています。Lexusでは第Ⅳ問へ修復待ち注記を追加しました。

## 6. Ⅳ(2)(3) 積分の省略・不要な補助関数

原本p010の積和公式までの段階がraw/学習者から省略され、積分式からいきなり原始関数へ飛びます。`2sin²θ(cos3θ+1)=(1−cos2θ)(cos3θ+1)` と積和の展開を示してください。学習者が行幅対策だけに導入したA(θ),B(θ)は、後で幾何の点A,Bも使うため紛らわしいです。意味のまとまった1つのalignedで積分し、区間代入を明記。答え `π/3−9√3/20` は原本p010と独立数値積分で確認済み。古い `3π/8` へ戻さないでください。

原本p010の最初の積分行には4x²と読める箇所がある一方、問題・直前の断面積・直後の置換式は4x³です。これは問題から確定する次数3へ現在正しく整っています。原本誤植とHTML転記不備を混同せず、補正来歴へ記録してください。p012末尾の回転軸「x軸」と読める箇所も、問題と本文で指定されたz軸とは異なります。現在のHTMLのz軸は正しいので戻さないこと。

(3)の主解法を原本と同じ置換積分にするなら、(2)のどの原始関数を区間0〜πで使うか示します。奇関数の別解を主解法に置換するなら明確に編集記録を残す。答えπ−Vへ飛ぶだけにはしないでください。

## 7. Ⅳ(4) 両端の一致だけで線分全体の包含を結論しない

原本p012にはC′D′のxz平面への射影C″D″とグラフ図1を使い、線分全体が上面以下であることを確認する過程があります。この橋渡しがraw/学習者から消えています。Lexusの新規図ではこの確認を下段に可視化しました。

`u_L=cos(120°+α)`, `u_R=cos(120°−α)` とすると、0≦α≦60°で `−1≦u_L≦−1/2≦u_R≦1/2`。hは[−1,−1/2]で増加、[−1/2,1/2]で減少し、両端値はh(u_L)=h(u_R)=cos3α+1。したがって間の全点でh(u)≧cos3α+1となり、回転した長方形全体がSに含まれます。回転で円板内に留まり高さも保存されること、α=0,60°の退化断面も問題ないことを確認する。

**S全体が120度回転に不変、という主張ではありません。** 反例は(−1/2,0,1)∈Sを120度回した(1/4,−√3/4,1)で、この点の上面はh(1/4)=5/16<1です。analysisの「回転対称」「120度回転不変性」や学習者冒頭の表現は、単位円周上の端点の高さの一致と、回転したTのSへの包含へ限定して説明してください。正しい最終体積Vを変更する必要はありません。

## 8. 編集の構造・目的タグ・分析の前提

- 一般的な見出し「解説《…》」を次々の数式の目的タグへそのまま流用しない。既存ライブラリーから「極方程式」「共有点の条件」「微分計算」「場合分け」「因数の組」「確率の漸化式」「部分和」「断面積」「置換積分」「回転後の座標」等、実際の目的を表す語を選び、なければ重複検査後に追加。
- 全問の講評が第Ⅳ問の解説末尾へ割り付けられています。Ⅳ固有の導出と全試験の講評を明確に区別し、講評からⅠ〜Ⅲの解法を重複転載しない。削る場合はraw転記を維持して学習者版だけの編集として記録。
- 記述式のwritten_areaを分析HTMLが「マーク欄1」と呼ぶ箇所を、評価形式に応じた語へ修正。出題形式は全13小問のうちⅣ(1)だけ答えのみ、他は導出を要求しています。
- Ⅳ(3)の「独立に回収」は(2)の数値V不要という意味で、断面積の考え方まで不要という意味ではありません。現在の5分仮時間に何を含めるか担当者が確認。時間/配点/優先度を本作業で独断変更しないこと。
- 現在の暫定目標は苦手39点（初期ルート88分／最大同得点の別ルート83.5分）、得意68点（最大86点86.5分、初期70点68.5分）。全13問の判断時間を一度計上する46分／11.5分を維持。独立全探索と整合している。大学公表点や合格最低点とは呼ばない。公式Web/要項の新規再照合は今回していません。

## 9. 修復手順と完了報告

1. `source-html/reconstruction.json`、`source-html/editorial-explanations.json`、`analysis.json`、`issues.json`を正本として修復。生成HTMLだけを直さない。
2. 原本17ページと全13小問を再照合。question-reader、public-candidate/questions、internal/source-answers、internal/answers、derived、previewを再生成する。
3. `3π/8`、4x²の取り違え、x軸回転、古い増減表セル、未定義、用途のないA(θ)/B(θ)/T_n、S全体の回転不変という断定を文脈付きで残存検索。原本誤植は監査資料に残しても教材本文へ復活させない。
4. 4/7/14通り、確率遷移、共有点の境界、体積π/3−9√3/20、全体π、回転した線分全体の包含、目標点の前提閉包と時間を独立検算。package validatorとTeX監査を実施。
5. PC/390pxで3ページ、表のセル・見出し・斜線/罫線、数式改行・用途、全図のラベルを目視。印刷は機械検証とPDF/印刷プレビュー実機目視を分けて報告。
6. restricted cropを公開用へコピーしない。人間編集SVG/trioを保護し、needs_human_review/rights/approvedをAIだけで解除しない。判断不能はopen issueのまま残す。
7. Gitは対象packageと必要最小限の共通修正だけ。他担当・別大学・main/本番を勝手に変更しない。
8. 報告に原文→修正、原本ページ、正本ファイル、検算、再生成/テスト、未解決、再取込可能なpackage IDを明記。Lexusの注記は正本修復・再取込・検証後にのみ解除する。
