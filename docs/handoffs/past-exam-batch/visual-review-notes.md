# 追加の画面点検所見 — 修正担当向け

本ファイルは手動追記用。importerの再実行で上書きしない。修正済み項目も、根拠と修正コミットを添えて履歴を残す。

## 獨協医科大学 2025年度・前期・一次・数学

対象ID：`dokkyo-medical-2025-general-early-mathematics`。

2026-09-09の図版作成時に確認。図の座標・色配置条件はテスト済みだが、解説全文の原本照合ではない。

1. 第4問の散文に `PR=e^{√k}`、`t=e^{√k}` のような疑似TeXがそのまま見えている。第1問にも `ₙ₋₁C₂` という組合せ記号が通常のテキストで残る。元の学習者向け解説でインライン数式を構造化し、本文と同じKaTeX書体で出す。内容や式を勝手に省略しない。
2. 第2問(2)は「まず絶対値和でkを絞る」という数式タグが、後半の共円条件・最終比の計算にも繰り返されている。各計算の目的に合う既存タグへ分割する。第1・3・4問も、見出しを機械的にそのままタグにした箇所の意味を点検する。
3. 独自SVG 14図を登録済み。色チップの図は角柱の透視図ではなく、側面の隣接・周回順を示す模式図である。本文との対応を確認し、必要なら説明を補う。元のrestricted cropへ戻さない。
4. 目標点の保留理由は大学別修正依頼を参照。弱点側候補で `math-q3-3-ii` が前提となる `math-q3-3-i` なしに選ばれている。スコアを推測で埋めず、元の方針と依存関係を修復する。

今回の検証：SVG14図すべてラスタライズし目視。PCと390pxモバイルでページ横幅超過なし、全14図が読込済み。新規図を含む印刷プレビューの目視は未実施なので、修正担当の印刷確認に含める。

## 獨協医科大学 2025年度・前期・一次・物理 — 元データ修正依頼

対象ID：`dokkyo-medical-2025-general-early-physics`。
元パッケージ：`C:/---hp/shidai-igakubu-gokaku-dokuhon/projects/universities/dokkyo-medical/past-exams/working/2025/general-early/first-stage/physics`。

### 最優先：第4問・問3〜4の電流条件の不一致

- `source-html/reconstruction.json` の問題 `questions-p012`（実際のID先頭は対象ID）にある問3・問4は、電流条件が `\frac16 I_0`。`generated/public-candidate/questions/major-question-04.html` にも残っている。
- `source-html/editorial-explanations.json` の第4問・問3では条件が `I_0`、式が `0=RI_0-V_1'+V_2'`。解答は問3が②（1/9）、問4が③（7/108）。
- 元 `issues.json` の `source-transcription-correction-review` には解答p006を原本の `I_0` に直したと記録されているが、問題側が追随していない。問題原本p012も必ず照合し、どちらが正しいか確定すること。解答だけに合わせて機械置換しない。
- 計算による不一致の根拠：解説の接続・容量を前提とすると、`I_0=V/(2R)`、初期総電荷は `2CV/3`。条件が `I_0/6` なら `V_1'-V_2'=V/12` なので `V_2'=7V/36`、`Q_2=7CV/18`、ジュール熱は `7CV²/48`。後者は提示選択肢にない。一方 `I_0` なら `Q_2=CV/9`、熱 `7CV²/108` で現解答と一致する。これは原本照合の代わりではなく、同期漏れを裏付ける検算。
- ステージングの問題・解説の第4問冒頭には、問3〜4を演習・採点対象外とする注意書きを追加した。正本修復・再取り込み・検証後にのみ、Lexus側 `frontend/src/lib/pastExamReviewNotices.mjs` のこのIDのゲートを解除する。原本、選択肢、解答キー、分析の評価・依存関係をまとめて点検する。

### 未確定の3図に必要な情報

1. `q2-force-graph-options`：問題p007。①〜⑥それぞれの軸範囲、原点・端点・不連続、符号、増減・曲がり方、番号との対応を意味データとして記録する。正答の直線だけ分かっても、残る5選択肢は作れない。
2. `q4-rc-circuit`：問題p011。電池極性、R₁/R₂・C₁/C₂・S₁/S₂の全端子と接続ノード、初期スイッチ開状態を記録する。上記の電流条件不一致も解決する。見た目が似た回路を推測して代用しない。
3. `q5-speed-distributions`：問題p014。図2・図3の軸目盛と単位、Ar/X/破線の対応、ピークの値と線種を記録する。現解説はArのピーク200/400 m/s、Xを約300 m/sとして分子量約71→Krとしているが、本文だけではこれらの読み取り値は確認できない。原本で確認し、粗い数値を正確な測定値と混同しない。

### 修復・報告の手順

元リポジトリのAGENTS.mdと該当過去問スキルを読み、このパッケージだけを修復する。`reconstruction.json`、`editorial-explanations.json`、必要な分析データ・issuesを正本から修正し、public-candidate/questions、internal/source-answers、learner版など関連生成物を再生成する。古い条件を全ページで検索して同期漏れを検査する。原本p001〜p015の問題と全解説を対象に、今回列挙した箇所以外の不備も監査する。根拠不足はopen issueのまま残す。

制限付きクロップは配信資産に戻さず、確認済み条件から独自SVGを作成する。今回描いた8図は別生成スクリプト・manifestで保管しており、必要なものだけ修正する。科目担当レビュー・権利状態を自動で承認済みにしない。本番・mainへのpushや無関係ファイルの変更は行わない。

修正後はvalidator、数式描画、選択肢順、問題と解説の一致、PC/390pxモバイル、印刷を確認し、正本の変更位置・原文と修正後・根拠ページ・検算・生成物・未解決事項・コミットを報告する。Lexus側で対象IDを再取り込みし、既存独自解説は上書きしない。

今回の8図は全PNGを目視、PC/モバイル読込を確認。新規図の印刷プレビュー目視は未実施。
