# 追加の画面点検所見 — 修正担当向け

本ファイルは手動追記用。importerの再実行で上書きしない。修正済み項目も、根拠と修正コミットを添えて履歴を残す。

## 獨協医科大学 2025年度・前期・一次・数学

対象ID：`dokkyo-medical-2025-general-early-mathematics`。

2026-09-09の図版作成時に確認。図の座標・色配置条件はテスト済みだが、解説全文の原本照合ではない。

1. 第4問の散文に `PR=e^{√k}`、`t=e^{√k}` のような疑似TeXがそのまま見えている。第1問にも `ₙ₋₁C₂` という組合せ記号が通常のテキストで残る。元の学習者向け解説でインライン数式を構造化し、本文と同じKaTeX書体で出す。内容や式を勝手に省略しない。
2. 第2問(2)は「まず絶対値和でkを絞る」という数式タグが、後半の共円条件・最終比の計算にも繰り返されている。各計算の目的に合う既存タグへ分割する。第1・3・4問も、見出しを機械的にそのままタグにした箇所の意味を点検する。
3. 独自SVG 14図を登録済み。色チップの図は角柱の透視図ではなく、側面の隣接・周回順を示す模式図である。本文との対応を確認し、必要なら説明を補う。元のrestricted cropへ戻さない。
4. 目標点の保留理由は大学別修正依頼を参照。弱点側候補で `math-q3-3-ii` が前提となる `math-q3-3-i` なしに選ばれている。スコアを推測で埋めず、元の方針と依存関係を修復する。

今回の検証：SVG14図すべてラスタライズし目視。PCと390pxモバイルでページ横幅超過なし、全14図が読込済み。新規図を含む印刷プレビューの目視は未実施なので、修正担当の印刷確認に含める。

## 獨協医科大学 2025年度・前期・一次・物理 — 元データ修正依頼

対象ID：`dokkyo-medical-2025-general-early-physics`。
元パッケージ：`C:/---hp/shidai-igakubu-gokaku-dokuhon/projects/universities/dokkyo-medical/past-exams/working/2025/general-early/first-stage/physics`。

### 最優先：第4問・問3〜4の電流条件の不一致

- `source-html/reconstruction.json` の問題 `questions-p012`（実際のID先頭は対象ID）にある問3・問4は、電流条件が `\frac16 I_0`。`generated/public-candidate/questions/major-question-04.html` にも残っている。
- `source-html/editorial-explanations.json` の第4問・問3では条件が `I_0`、式が `0=RI_0-V_1'+V_2'`。解答は問3が②（1/9）、問4が③（7/108）。
- 元 `issues.json` の `source-transcription-correction-review` には解答p006を原本の `I_0` に直したと記録されているが、問題側が追随していない。問題原本p012も必ず照合し、どちらが正しいか確定すること。解答だけに合わせて機械置換しない。
- 計算による不一致の根拠：解説の接続・容量を前提とすると、`I_0=V/(2R)`、初期総電荷は `2CV/3`。条件が `I_0/6` なら `V_1'-V_2'=V/12` なので `V_2'=7V/36`、`Q_2=7CV/18`、ジュール熱は `7CV²/48`。後者は提示選択肢にない。一方 `I_0` なら `Q_2=CV/9`、熱 `7CV²/108` で現解答と一致する。これは原本照合の代わりではなく、同期漏れを裏付ける検算。
- ステージングの問題・解説の第4問冒頭には、問3〜4を演習・採点対象外とする注意書きを追加した。正本修復・再取り込み・検証後にのみ、Lexus側 `frontend/src/lib/pastExamReviewNotices.mjs` のこのIDのゲートを解除する。原本、選択肢、解答キー、分析の評価・依存関係をまとめて点検する。

### 未確定の3図に必要な情報

1. `q2-force-graph-options`：問題p007。①〜⑥それぞれの軸範囲、原点・端点・不連続、符号、増減・曲がり方、番号との対応を意味データとして記録する。正答の直線だけ分かっても、残る5選択肢は作れない。
2. `q4-rc-circuit`：問題p011。電池極性、R₁/R₂・C₁/C₂・S₁/S₂の全端子と接続ノード、初期スイッチ開状態を記録する。上記の電流条件不一致も解決する。見た目が似た回路を推測して代用しない。
3. `q5-speed-distributions`：問題p014。図2・図3の軸目盛と単位、Ar/X/破線の対応、ピークの値と線種を記録する。現解説はArのピーク200/400 m/s、Xを約300 m/sとして分子量約71→Krとしているが、本文だけではこれらの読み取り値は確認できない。原本で確認し、粗い数値を正確な測定値と混同しない。

### 修復・報告の手順

元リポジトリのAGENTS.mdと該当過去問スキルを読み、このパッケージだけを修復する。`reconstruction.json`、`editorial-explanations.json`、必要な分析データ・issuesを正本から修正し、public-candidate/questions、internal/source-answers、learner版など関連生成物を再生成する。古い条件を全ページで検索して同期漏れを検査する。原本p001〜p015の問題と全解説を対象に、今回列挙した箇所以外の不備も監査する。根拠不足はopen issueのまま残す。

制限付きクロップは配信資産に戻さず、確認済み条件から独自SVGを作成する。今回描いた8図は別生成スクリプト・manifestで保管しており、必要なものだけ修正する。科目担当レビュー・権利状態を自動で承認済みにしない。本番・mainへのpushや無関係ファイルの変更は行わない。

修正後はvalidator、数式描画、選択肢順、問題と解説の一致、PC/390pxモバイル、印刷を確認し、正本の変更位置・原文と修正後・根拠ページ・検算・生成物・未解決事項・コミットを報告する。Lexus側で対象IDを再取り込みし、既存独自解説は上書きしない。

今回の8図は全PNGを目視、PC/モバイル読込を確認。新規図の印刷プレビュー目視は未実施。

## 藤田医科大学 2025年度・前期・一次・数学 — 元データ修正依頼

対象ID：`fujita-health-2025-general-early-mathematics`。
元パッケージ：`C:/---hp/shidai-igakubu-gokaku-dokuhon/projects/universities/fujita-health/past-exams/working/2025/general-early/first-stage/mathematics`。

図の作成時に次の説明不備を検出した。正本を修復して関連HTMLを再生成すること。今回は元ファイルを書き換えていない。5図は公開問題の条件から独自作図し、制限付きクロップを利用していない。

1. **第1問(1)：回転領域と積分式の不一致。** `reconstruction.json` の解答p001は `2π∫₀¹ x(2x−x³) dx = 4π/3` としているが、この積分は `14π/15` であり等号が誤り。左右両方をy軸回転する問題条件では、半径rの占有区間は `[-r,r−r³]` と `[r³−r,r]`。0〜1では重なるため、和集合は `[-r,r]` となる。正しい円筒殻法は `2π∫₀¹ r·2r dr = 4π/3`。単純な2倍では重複計数する。円板法なら回転後の子午断面を使い `π∫₋₁¹ (1−y²)dy` と書ける。現在の学習者向け解説は「円板の差」「原本の積分を整理すると」で途中を省略しており、読者が確認不能。原本の意図を照合し、左右領域の和集合→断面→積分の一貫した説明へ。追加図はこの和集合を示す。
2. **第1問(2)：1となる偏角条件を修正。** 学習者向け解説は「n倍がπの整数倍」と書くが、それでは−1も含む。条件は2πの整数倍である。偏角差29π/18、`29n=36m`、最小n=36へつなぐ。原本転記側p002では2mπであり、編集解説で条件を落としている。
3. **第1問(8)：「第1問の確認」のt>0は誤り。** `t=3^x−3^(−x)` は実数全体を動く。t=−3/2で最小値−1/4をとる本文と、p006の確認欄「指数置換のt>0」が矛盾する。別問題の指数置換の定型文を流用していないか他の確認欄も点検する。単調性・両端の極限、あるいは逆変換で値域を示す。t=−3/2はx=−log₃2で実現する。
4. **第1問(3)・(7)など：必要な導出が省略されている。** (3)は判別式→許容範囲aが−1以下または5以上→`4(a−1)²−14`→境界値の比較を記す。(7)は「相似な三角形から角を移します」だけでは135度を追えない。`OA=QB=a+b, OQ=b, OP=a, PA=b`とそろえ、α・βの定義、tanの差角公式、補角へつなぐ。原本解法を黙って別解へ変更せず、採用した方法の理由を示す。
5. **第2問：囲まれた部分の上端を明確に。** 「上側の放物線」は二つのうち高い方とも読める。接点間で囲まれた有界領域は、接線より上・二放物線の低い方より下。左区間はC₁、右はC₂であり、元の積分区間はこれと整合する。追加図は `u=x+2k, v=y−ℓ(x)` の面積保存変換による模式図。本文にこの図を使う理由・元のx座標との対応を簡潔に添えるか、同じ条件から一般のkの配置を追加する。単に図の変数をx,yへ改名してはならない。
6. **目標点：元の丸め規則を確認。** 自動検査は苦手側72点と計算上72.5点（整数丸めなら73点）の不一致を検出した。方針を確認して正本の数値と計算を一致させる。ステージングは目標値を保留しているため、推測で解除しない。

第1問(1)(2)(8)はページ上にも説明確認中の注意書きを追加した。上記正本修復・再取込・内容検査の後にだけ、Lexus側 `frontend/src/lib/pastExamReviewNotices.mjs` の該当ゲートを解除する。問題条件や正しい解答値を勝手に変更しない。

元リポジトリのAGENTS.mdと該当スキルを読み、原本全13ページと問題・解答・学習者向け解説・分析の同期を監査する。疑似TeX、不要式、計算タグの意味、条件の必要十分性、代入先、絶対値などの省略も点検。原本→正本JSON→生成HTMLの順に修復し、根拠のない箇所はissuesに残す。大学別自動修正依頼・upstream-issues.mdも併読すること。科目レビューと権利を自動承認せず、main・本番・他担当ファイルを変更しない。

完了時に変更前後、根拠ページ・検算、再生成物、PC/390px/数式/図/印刷検証、未解決事項とコミットを報告する。独自SVG5図は生成コード・manifest・意味テストを保存済みで、人が編集した資産を上書きしない共通handoff機構を利用している。

## 藤田医科大学 2025年度・前期・一次・物理 — 元データ修正依頼

対象ID：`fujita-health-2025-general-early-physics`。
元パッケージ：`C:/---hp/shidai-igakubu-gokaku-dokuhon/projects/universities/fujita-health/past-exams/working/2025/general-early/first-stage/physics`。

### 第4問・問9〜10：解釈上の重要な限定が編集解説から消えている

`source-html/reconstruction.json` の解答p009末尾には、問9では `v′P≧0` が必要で、問10には再衝突の問題もあるため、元解答は `Ymax` を0〜3で**数学的にグラフ化する問題**として扱った、という注記がある。一方 `editorial-explanations.json` のp008・p009は、式を無条件に物理的な最高点として紹介し、この限定を引き継いでいない。

上向きを正とした直後速度は `v′P=((e²+2e)M−m)/(M+m)·√(2gH)`。e=1、x=M/mでは `(3x−1)/(x+1)·√(2gH)` なので、指定範囲内の0<x<1/3では下向きとなる。下向きの初速度を二乗しただけで、その後にv=0の最高点が実現するとは言えない。問9の `0²−v′P²=−2gYmax` には運動区間と向きの前提が必要。問題p004はPと床の反発条件を明記しておらず、再衝突の扱いも原本と照合すること。元注記の `Ymax≧H` という再衝突の説明も、そのまま正解と断定せず根拠を確認する。

修正担当は問題p004、解答p008・p009、学習者版p008・p009を一緒に監査し、(a)元の設問条件、(b)元解答が採用した解釈、(c)物理的な適用範囲を分けて説明する。確定しない条件をM>mなどの追記で黙って補わない。数学的グラフの `Ymax/H=((3x−1)/(x+1))²` 自体はx=0で1、x=1/3で0、x=3で4となり、独立検算済み。

今回の `a4-answer-graph` と `a4-solution-graph` は、元注記に基づく**数式の可視化だけ**として作図し、alt・caption・図内の見出しでその限定を明示した。問題・解説の第4問には、正式教材としての演習・採点利用を保留する注意書きもある。修復・再取り込み・科目レビュー後にのみ `frontend/src/lib/pastExamReviewNotices.mjs` の該当ゲートを解除する。

### その他の検出・点検事項

1. 学習者版p006では、問10の `a4-answer-graph` が問1の説明直後に挿入されている。元の解答一覧と詳細説明の区別を明確にし、少なくとも問1の図と誤認しない場所へ移す。`a3-force-components` も問4用なのに問7の後、`a2-lens-geometry` も主に問1用なのに問4の後に位置する。ページ画像の順ではなく小問の説明と対応させて配置する。Lexus側は現位置を削除せず、解答グラフや力の図のcaptionに対象小問を明記している。
2. 分析の苦手側プラン `now_plus_later` に、`phys-q2-6` を選ばず前提依存する `phys-q2-7` が含まれる。元の問題選択・配点・時間を監査して整合させる。依存関係だけを消してvalidatorを通さない。目標点セクションは現在保留中。
3. 多くの学習者向け解説が「考え方」1行と答えのみになっている。張力の作用方向と点C周りのモーメント、ニュートンリングの位相と不等式端点、電場での力積、衝突前後の符号付き速度→速さの順を、原本の導出と照合して補う。とくに「代入する」「近似する」には対象の式と条件を示す。物理的前提を省略して解法だけを短縮しない。
4. 問題図では第3問の磁場方向が設問の答えなので、+zの磁場記号や半円軌道を入れない。解説図だけに+z・半円・力成分を描画した。問題図の「z軸正方向：紙面手前」は座標の定義であり、磁場方向を与えるものではない。
5. 新規10図は問題条件・式から独自作成。第1問はAが上右、Bが上左、Cが下左、ひもが下向きに張る構成で、問3のモーメント `H T₁ sinθ₁+d T₁ cosθ₁` と整合させた。第2問は球面の幾何関係から描き、空気層に拡大図を添えた。編集時も点・符号・角度基準を維持する。

### 修復手順と報告

元リポジトリのAGENTS.mdと該当過去問スキルを読み、このパッケージの原本13ページ・全28小問を監査する。`reconstruction.json`、`editorial-explanations.json`、analysis/derived、issuesを正本から修正し、public-candidate/questions・internal/source-answers・learner版・分析HTMLを再生成する。全生成物で欠落注記・旧条件・小問対応を検索し、同期漏れを検査する。大学別自動依頼とupstream-issues.mdも確認する。未確定事項はopen issueとして残す。

restrictedクロップは配信せず、新規SVG/manifest/生成コードを維持する。手編集済み資産を上書きしない。権利・科目担当レビューを自動承認しない。本番・main・他担当ファイルへ変更を広げない。

完了時は変更前後、根拠の原本ページ・物理計算、再生成ファイル、validator・TeX・PC/390px・図表・印刷の結果、未解決事項とコミットを報告する。Lexus側は対象IDだけを再取り込み、承認済み独自解説5件を保護する。
