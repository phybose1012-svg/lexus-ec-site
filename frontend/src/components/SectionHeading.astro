---
interface Props {
  label: string;
  title: string;
  subtitle?: string;
  id?: string;
  editableId?: string;
}

const { label, title, subtitle, id, editableId } = Astro.props;
const titleLines = title.split("\n");
---

<header class="section-heading" id={id}>
  <img src="/assets/legacy/wp-content/uploads/2024/11/背景ロゴだらけメニュー透明3-1024x74.png" alt="" width="800" height="58" loading="lazy" decoding="async" />
  <p>{label}</p>
  <h2 data-admin-editable={editableId || undefined}>
    {titleLines.map((line, index) => (
      <>
        {index > 0 && <br />}
        {line}
      </>
    ))}
  </h2>
  {subtitle && <small>{subtitle}</small>}
</header>
