import type { Point2D } from "./shapeBuilders";
/**
 * 補正のしきい値。**決め打ちを散らさないため、ここが唯一の定義元。**
 *
 * 長さの比は、断りがなければ「その軌跡自身の外接矩形の対角線」に対する割合。
 * 軌跡の大きさに追随するので、キャンバスの解像度や拡大率が変わっても同じ絵は
 * 同じように読める（strokeRecognition.ts と同じ決まり）。
 */
export declare const STROKE_CORRECTION: {
    /** 関数 y = f(x) として読み直すときの標本数（x 等間隔）。奇数にして中央を標本に乗せる。 */
    readonly SAMPLE_COUNT: 65;
    /**
     * x の後戻りをどこまで許すか（x 幅に対する割合）。
     *
     * これを超えたら「おおむね関数的」ではない＝縦に折り返した軌跡なので、
     * どの補正も出さない。
     *
     * 見るのは**いちばん深く戻った量**であって、戻った量の合計ではない。
     * 手の震えは進みながら細かく前後するので、合計で測ると「まっすぐ右へ
     * 引いた放物線」でも数 % 積み上がって門番に引っかかる（実測 4.8%）。
     * 折り返した軌跡は 1 回で幅ぶん戻るので、深さで測れば取り違えない
     * （横向きの放物線で 100%）。
     */
    readonly MONOTONIC_BACKTRACK_FRACTION: 0.06;
    /** x 幅が対角線のこれ未満なら、縦に近い軌跡。関数としては読めない。 */
    readonly MIN_WIDTH_FRACTION: 0.35;
    /**
     * 曲がりの下限（両端を結んだ弦からの最大ずれ / 対角線）。
     *
     * まっすぐな線は「点対称」も「周期」も形式上は満たすが、補正しても何も
     * 変わらない。候補バーに意味のない札が並ぶだけなので手前で落とす。
     */
    readonly MIN_BEND_FRACTION: 0.05;
    /**
     * 補正で動かす量（RMS / 対角線）の上限。**もう 1 つの門番。**
     *
     * 非対称の割合（MIRROR_MAX_SHARE ほか）とは別のことを見ている——割合が
     * 小さくても、当てはめ自体が形を説明できていなければ残差は大きい。
     * 「だらだら下る波」がまさにそれで、波を中心について見れば確かに奇関数
     * っぽいので割合は 0.023 まで下がるが、当てた三次は波にまるで乗っていない
     * （動かす量が 0.055）。押した人の目に「別の形になった」としか映らない
     * 行き過ぎを、ここで断つ。
     *
     * コーパスで測った値は、出すべき側の最悪が 0.019、上の「割合をすり抜けて
     * くる側」の最小が 0.050。間を取って 0.03（前は 0.05 に置いていて、
     * すり抜け側と margin ゼロで隣り合っていた）。
     */
    readonly MAX_SHIFT_FRACTION: 0.03;
    /**
     * 候補として出す確信度の下限。
     *
     * 採否そのものは 2 つの門番（MIRROR_MAX_SHARE ほかと MAX_SHIFT_FRACTION）が
     * 決める。ここはその保険——**両方のすれすれを重ねて通ってきたもの**だけを
     * 落とす。どちらか片方でも余裕があれば、この値には引っかからない。
     *
     * 高くしすぎないこと。確信度は 2 つの門番までの余裕の積なので、ここを上げると
     * 門番を黙って厳しくしたのと同じになる。出すべきコーパスの最低は 0.134
     * （震え 5px の「底が平たい対称の曲線」）なので、その 1/2 以下に置く。
     */
    readonly MIN_CONFIDENCE: 0.05;
    /** 対称軸・対称中心を探す範囲（x 幅に対する、中央からの片側割合）。 */
    readonly AXIS_SEARCH_FRACTION: 0.15;
    /** 軸探索の刻み数（粗探し）。 */
    readonly AXIS_SEARCH_STEPS: 61;
    /** 詰め直しの刻み数と回数。粗探しの刻みを結果に残さないための 2 段。 */
    readonly REFINE_STEPS: 21;
    readonly REFINE_PASSES: 2;
    /**
     * 軸をはさんだ左右の腕の、短い方 / 長い方。これを割ったら札を出さない。
     *
     * **実機の不具合 (b) の本体。** 腕の比が 0.4 : 1.0 だった放物線で、選んだ
     * あとの左右の端点が 109px ずれていた。偶関数は軸について厳密に対称でも、
     * 描かれた範囲が軸について非対称なら端は揃わない。揃えるには短い腕に
     * 合わせて切るしかなく、切る量は「長い腕 × (1 - この比)」。0.85 なら
     * 長い腕の 15% までしか捨てない＝なぞった線が目に見えて短くはならない。
     * これ以上に腕が偏ったなぞりは、**左右を見比べたとは言えない**ので、
     * 切って辻褄を合わせるのではなく札そのものを出さない。
     */
    readonly MIN_ARM_RATIO: 0.85;
    /**
     * 線対称に当てる多項式の次数。制約形は偶数番だけ（P0 P2 P4）。
     *
     * **次数を上げすぎないこと。** 自由度が増えるほど残差は減るが、高い次数は
     * 端で暴れるので、なぞりの端 1 点の震えに引かれて曲線がそこだけ跳ねる。
     * 放物線は P2 で足り、P4 は「底が平たい対称の曲線」を通すための余裕。
     */
    readonly MIRROR_DEGREE: 4;
    /**
     * 点対称に当てる多項式の次数を、**低い方から順に試す梯子**。
     * 制約形は P0（中心の高さ）と奇数番（3 なら P1 P3、5 なら P1 P3 P5）。
     *
     * P1 は残すが、**対称性の証拠には数えない**。三次関数の変曲点まわりは
     * a·u³ + b·u で、傾き b が無いと山と谷のある三次が通らないため必要な項で
     * ある一方、傾きは「ただの坂」でも出る。証拠として数えるのは、外した
     * 偶数番（P2 P4）を足したときに当てはまりがどれだけ良くなるか、それだけ
     * ——ルジャンドルは直交しているので、P0 P1 を落とした残り（＝ detrend した
     * 波）に奇数番を当てるのと、まとめて当てるのは同じ答えになる。
     *
     * ## なぜ 3 だけでは足りなかったか（2026-09-01・実機の報告）
     *
     * 「タンジェントのような曲線には点対称が効くのに、**サインカーブには
     * ほとんど効かない**」。測ったらそのとおりだった——極値が 2 つ以上ある
     * 奇関数は三次では**原理的に表せない**ので、当てはめが形に乗らず、
     * もう 1 つの門番（MAX_SHIFT_FRACTION）が止めていた。
     *
     * | 形 | 次数 3 の動かす量 | 上限 | 札 |
     * |---|---|---|---|
     * | 三次（変曲点まわり） | 0.013 | 0.03 | 出る |
     * | サイン 1 周期 | 0.024 | 0.03 | かろうじて出る |
     * | サイン 1 周期（背の高いもの） | 0.033 | 0.03 | **出ない** |
     * | サイン 1.25 周期 | 0.036 | 0.03 | **出ない**（24 本中 2 本だけ） |
     * | 五次風の奇関数（極値 4 つ） | 0.056 | 0.03 | **出ない** |
     *
     * ## 次数は上げるが、いつでも上げるのではない
     *
     * **一律に 5 へ上げてはいけない。** 外す偶数番が P2 だけから P2 P4 の 2 本に
     * 増えるので、外した成分に手の震えが 2 本ぶん乗る。三次関数の割合が
     * 0.031 → 0.094 まで上がり（上限 0.075）、**いま通っている三次が落ちる**。
     *
     * そこで**低い次数で足りるならそれを使う**。3 で当てて、動かす量が
     * POINT_ESCALATE_SHIFT_FRACTION を超えたときだけ 5 へ上げる——「その次数では
     * 形を説明できなかった」という理由があるときだけ自由度を買う。
     *
     * **買っても甘くならない。** 比べる相手（一般形）も同じ次数へ上がるので、
     * 自由度を買うと対称性の問いはむしろ厳しくなる。実測でも、次数を上げて
     * すり抜けようとする形はここで落ちる——「だらだら下る波」の割合は
     * 次数 3 で 0.023（すり抜ける）だったものが、次数 5 では 0.394 になる。
     *
     * **5 で止める。** 7 まで許すと端が暴れて、なぞりの端の震えに引かれた
     * 曲線が端だけ跳ねる（下の POINT_END_GUARD_FRACTION も見よ）。手で描く
     * 奇関数は極値 4 つ＝五次で足りる。
     */
    readonly POINT_DEGREE_LADDER: readonly [3, 5];
    /**
     * 次数を 1 段上げる境目（MAX_SHIFT_FRACTION に対する割合）。
     *
     * 動かす量がこれを超えたら「その次数では形を説明できていない」と読んで
     * 次を試す。**この境目の仕事は 1 つだけ**——次数 3 のままでは採否の上限
     * （0.03）に引っかかる形を、確実に 5 へ送ること。上限そのもの（1.0）では
     * なく手前に置くのは、震えの引き当てで上限すれすれを行き来する形を
     * 取りこぼさないため。
     *
     * 次数 3 での動かす量を、コーパスの点対称群で測った幅——
     *
     * | 形 | 次数 3 の動かす量 | 3 のままでよいか |
     * |---|---|---|
     * | 三次（変曲点まわり） | 0.0056〜0.0130 | よい |
     * | タンジェント風 | 0.0057〜0.0139 | よい |
     * | サイン 1 周期（背の低いもの） | 0.0085〜0.0154 | よい |
     * | サイン 1 周期 | 0.0141〜0.0244 | どちらでも通る |
     * | サイン 1 周期（背の高いもの） | 0.0200〜0.0333 | **5 が要る** |
     * | サイン 1.25 周期 | 0.0277〜0.0357 | **5 が要る** |
     * | 五次風の奇関数 | 0.0467〜0.0555 | **5 が要る** |
     *
     * 0.7（＝ 0.021）は「3 のままでよい」群の上（0.0154）と「5 が要る」群の
     * 下（0.0200）の間に入る。**サイン 1 周期だけは種によって 3 と 5 に
     * 分かれるが、どちらの次数でも通る**ので、分かれても結果は変わらない。
     */
    readonly POINT_ESCALATE_SHIFT_FRACTION: 0.7;
    /**
     * 端の跳ねの上限（なぞりの起伏に対する割合）。**次数を上げた手当て。**
     *
     * 高い次数はデータの外側へ向かうほど自由に振れるので、なぞりの端の震えに
     * 引かれて曲線がそこだけ跳ねる。当てはめ全体の残差（動かす量）は 65 点で
     * 平均されるため、端の跳ねはそこにほとんど出てこない——**別に見張るしか
     * ない**。測り方は `endOvershoot`（なぞりが通った高さの帯からのはみ出し）。
     *
     * **心配は実在した。** 中がほぼ平らで端だけ急に立つ奇関数（|u|⁷ の形）で
     * 測ると、次数 3 では 0.148 だったはみ出しが、次数 5 では **0.745** に
     * 跳ねた（|u|¹¹ なら 0.118 → **0.859**）。どちらも 2 つの門番はすり抜ける
     * ——端だけ別の形になっていることは、割合にも動かす量にも出てこない。
     *
     * | | 次数 3 | 次数 5 |
     * |---|---|---|
     * | 通すべきコーパスの最悪 | 0.282（三次関数） | 0.171（五次風） |
     * | 中が平ら・端が急（|u|⁷） | 0.148 | 0.745 |
     * | 中が平ら・端が急（|u|¹¹） | 0.118 | 0.859 |
     *
     * 通すべき側の最悪 0.282 と、跳ねた側の最小 0.745 の間を取って 0.5。
     * **通すべきコーパスには一度も掛からない**——掛かるのは、なぞった高さの
     * 帯を半分ぶんもはみ出したときだけ。
     */
    readonly POINT_END_GUARD_FRACTION: 0.5;
    /** 周期に当てる波の本数（基本波と高調波）。 */
    readonly PERIOD_HARMONICS: 3;
    /**
     * 周期の比べ相手にする多項式の次数。
     *
     * 波の側は「定数 + 傾き + 高調波 3 本」で 2 + 6 = 8 自由度。**同じ自由度**の
     * 多項式（P0..P7）を相手にすれば、繰り返しという仮定にいくら払ったかだけが
     * 比に残る。
     */
    readonly PERIOD_GENERAL_DEGREE: 7;
    /**
     * 非対称の割合の上限（線対称）。**採否はこれで決まる。**
     *
     * 「制約が捨てた成分の大きさ ÷ その形自身の大きさ」——左右の食い違いが
     * 絵の何割か、という目で見たままの量（`forbiddenShare`）。
     *
     * 手元のコーパス（tests/strokeCorrection.test.ts の 2 群）を、震え 2.5/3.5/5px
     * ×種 8 通りで測った値——
     *
     * | | 線対称 | 点対称 | 周期 |
     * |---|---|---|---|
     * | 出すべき側の最悪 | 0.077 | 0.031 | 0.050 |
     * | 出してはいけない側の最小 | 0.208 | 0.023 | 0.154 |
     *
     * 線対称と周期は間がよく空いているので、真ん中よりやや厳しい側へ置けばよい。
     * **点対称だけは、この量ではすり抜けられる**——「だらだら下る波」は中心に
     * ついて見れば確かに奇関数っぽく、割合が 0.023 まで下がる。そちらは
     * MAX_SHIFT_FRACTION（当てた三次が波に乗っていないこと）が止める。
     * 門番が 2 つある理由がここにある。
     */
    readonly MIRROR_MAX_SHARE: 0.12;
    /** 非対称の割合の上限（点対称）。捨てた偶数次の大きさ ÷ その形自身の大きさ。 */
    readonly POINT_MAX_SHARE: 0.075;
    /** 周期から外れた成分の割合の上限。 */
    readonly PERIOD_MAX_SHARE: 0.1;
    /**
     * 残差比を取るときに、分母・分子へ等しく足す下駄（対角線に対する割合）。
     *
     * 素の比は、両方の残差がほとんど 0 のとき（計算で作った理想的な曲線など）に
     * 0/0 へ落ちて暴れる。目に見えない差——対角線の 0.3%、237px の軌跡で 0.7px
     * ——を両側に足しておけば、その領域では比が 1 に寄る。
     */
    readonly RESIDUAL_FLOOR_FRACTION: 0.003;
    /** 周期の粗探しの刻み数。許す周期の範囲を丸ごと舐める。 */
    readonly PERIOD_SEARCH_STEPS: 81;
    /**
     * これだけ繰り返していないと周期とは読まない（本数）。
     *
     * 「サイン 2 周期以上は読めてほしい」が要望なので 2。**下げてはいけない**
     * ——繰り返しが 2 本を割ると、高調波 3 本ぶんの自由度で「だんだん詰まって
     * いく波」まで通ってしまう（1.85 本の波で実際に通った）。
     */
    readonly MIN_PERIODS: 2;
    /** 波の細かさの上限（この本数を超える繰り返しは手の震えとみなす）。 */
    readonly MAX_PERIODS: 6;
    /** 波の振幅（傾きを抜いた残りの RMS / 対角線）の下限。 */
    readonly MIN_WAVE_AMPLITUDE_FRACTION: 0.02;
    /**
     * 1 周期ごとの振幅の揃い方（いちばん低い山 / いちばん高い山）。
     * **周期だけの 3 つめの門番**（2026-09-01）。
     *
     * 「1 周期ごとに厳密に同じ形」が周期の言い分なので、**振幅がだんだん育って
     * いく波は周期ではない**。ところが上の 2 つの門番はどちらもこれを見ていない
     * ——`share` は「多項式の方がよく当たるか」、`shift` は「当てはめが軌跡に
     * 乗っているか」で、育つ波はどちらも**すり抜ける**。高調波 3 本ぶんの自由度が
     * 育ちを部分的に吸ってしまい、残った分は震えに紛れる大きさにしかならない。
     *
     * 振幅 0.45 倍から 1.0 倍へ育つ 2.2 周期の波を、コーパスと同じ枠組み
     * （震え 2.5/3.5/5px × 種 8 通り = 24 本）で測ると——
     *
     * | | 割合 | 動かす量 | 振幅の揃い |
     * |---|---|---|---|
     * | 上限 | 0.1 | 0.03 | この値 |
     * | 育つ波（24 本の幅） | 0.000〜0.198 | 0.018〜0.030 | 0.623〜0.806 |
     * | サイン 2 周期 | 0.000〜0.050 | 0.008〜0.019 | 0.959〜0.995 |
     * | サイン 2.5 周期 | 0.000〜0.000 | 0.007〜0.017 | 0.922〜0.999 |
     * | 傾いたコサイン 2.2 周期 | 0.000〜0.000 | 0.006〜0.016 | 0.951〜0.999 |
     *
     * 割合も動かす量も 2 群が重なっている（**24 本中 6 本で札が出ていた**）が、
     * 振幅の揃い方だけは 0.806 と 0.922 できれいに割れる。通すべき側の最悪
     * 0.922 と、止めるべき側の最大 0.806 の間を取って 0.86。
     *
     * **MIN_PERIODS を下げて済ませることはできない。** あちらは「繰り返しが
     * 2 本を割ると、だんだん詰まっていく波まで通る」ための下限で、下げるのは
     * 逆方向（上の MIN_PERIODS のコメント）。育つ波は 2.2 本きちんと繰り返して
     * いるので、本数を数えても捕まらない。
     *
     * 測り方は `amplitudeConstancy`——当てた周期ぶんの窓を隙間なく敷いて、
     * 窓ごとの振幅（傾きを抜いた残りの RMS）を見比べるだけ。育つ波でも萎む波でも
     * 同じように出る（min / max なので向きに依らない）。
     */
    readonly PERIOD_MIN_AMPLITUDE_RATIO: 0.86;
};
export type StrokeCorrectionKind = "mirror" | "point" | "period";
/**
 * 何について対称にしたかの目印。**画面のガイド専用**（点線の軸・×印）。
 *
 * 当てはめが既に持っている値をそのまま返すだけで、新しい計算はしていない。
 * 図形そのものには入れない——書き出しに出さないと決めたので、図形の形へ
 * 混ぜてしまうと消す場所が無くなる（呼び出し側が画面にだけ描く）。
 */
export type StrokeSymmetryMarker = 
/** 線対称の軸。x = value の縦線。 */
{
    kind: "axis";
    x: number;
}
/** 点対称の中心。 */
 | {
    kind: "center";
    x: number;
    y: number;
};
export interface StrokeCorrection {
    kind: StrokeCorrectionKind;
    /** 候補バーに出す名前。 */
    label: string;
    /** 補正後の軌跡。描いた向きは保つ（線対称・点対称は左右等長に切りそろえる）。 */
    points: Point2D[];
    /** 0..1。対称だから当たったと言えるほど、かつ動かさずに済んだほど高い。 */
    confidence: number;
    /** 動かした量（RMS）／対角線。しきい値の意味が読めるように返す。 */
    shift: number;
    /** 制約が捨てた成分 ／ その形自身の大きさ。0 に近いほどその読みらしい。 */
    share: number;
    /** 対称の目印（画面のガイド用）。周期には無いので null。 */
    marker: StrokeSymmetryMarker | null;
}
/**
 * なぞった軌跡から、対称・周期の補正候補を確からしい順に返す。
 *
 * 空配列は「そう読める形ではない」。**確信の持てないものは返さない**——
 * 候補バーに並んだ札は押されるものなので、押した結果が「描いたつもりの形」
 * でなければ、選び直せることは慰めにならない。
 */
export declare function correctStroke(points: readonly Point2D[]): StrokeCorrection[];
/** 覗き窓が返す 1 つの読みぶん。採否の軸をそのまま出す。 */
export interface StrokeSymmetryReading {
    /** 残差比 ＝ 制約形 / 一般形。その仮定にいくら払ったか（震え基準）。 */
    ratio: number;
    /** 制約が捨てた成分 ／ その形自身の大きさ（形基準）。 */
    share: number;
    /** 動かす量（RMS）／対角線。当てはめが軌跡を説明できているか。 */
    shift: number;
    /** 実際に使った次数（点対称は梯子で上がる）。 */
    degree: number;
    /** 端の跳ね ／ なぞりの起伏。次数を上げたぶんの門番。 */
    endOvershoot: number;
    /**
     * 1 周期ごとの振幅の揃い方（いちばん低い山 / いちばん高い山）。**周期だけ。**
     *
     * 線対称・点対称には「毎周期」が無いので 1（＝文句なし）を返す。周期の
     * `endOvershoot` が 0 なのと同じ扱いで、**その読みが見ていない軸は、その
     * 読みを止めない値**を置く。
     */
    amplitudeConstancy: number;
}
/**
 * しきい値を測り直すための覗き窓。**採否をかけずに**判定の 2 軸だけを返す。
 *
 * MIRROR_MAX_SHARE ほかは「出すべき軌跡」と「出してはいけない軌跡」で share が
 * どう分かれるかを見て決めた。決め直すときも同じ道を通れるように、判定と同じ
 * コードから読めるようにしてある。ratio（震え基準の比）も並べて返すのは、
 * **なぜそちらを採らなかったかを測り直せるように**しておくため。
 */
export declare function inspectStroke(points: readonly Point2D[]): {
    mirror: StrokeSymmetryReading | null;
    point: StrokeSymmetryReading | null;
    period: StrokeSymmetryReading | null;
} | null;
